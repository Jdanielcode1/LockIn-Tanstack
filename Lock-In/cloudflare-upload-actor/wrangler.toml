name = "lock-in-upload-worker"
main = "src/index.ts"
compatibility_date = "2024-11-01"

# Enable observability for logs
[observability]
enabled = true

# R2 bucket binding for video uploads
[[r2_buckets]]
binding = "R2_BUCKET"
bucket_name = "lock-in-bucket-2"
