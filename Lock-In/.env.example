# Convex
CONVEX_DEPLOYMENT=

# Cloudflare Worker for video processing
VITE_WORKER_URL=https://your-worker.your-subdomain.workers.dev

# Cloudflare Upload Actor for multipart uploads (large files >100MB)
# Deploy cloudflare-upload-actor first, then add the URL here
VITE_UPLOAD_ACTOR_URL=https://lock-in-upload-actor.your-subdomain.workers.dev
